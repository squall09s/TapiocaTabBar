import UIKit

var greeting = "Hello, playground"

ZStack {
   HStack {
       Text("Hello")
       Text("World")
          .foregroundColor(.blue)
    }
   .foregroundColor(.red)
}
.foregroundColor(.green)

